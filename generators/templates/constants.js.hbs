// @flow

import { appCode } from 'config'

export const moduleName = '{{camelCase name}}'

export const LOAD_START = `${appCode}:${moduleName}/LOAD_START`
export const LOAD_SUCCESS = `${appCode}:${moduleName}/LOAD_SUCCESS`
export const LOAD_FAIL = `${appCode}:${moduleName}/LOAD_FAIL`
